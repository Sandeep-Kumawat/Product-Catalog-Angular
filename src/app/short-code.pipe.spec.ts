import { ShortCodePipe } from './short-code.pipe';

describe('ShortCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortCodePipe();
    expect(pipe).toBeTruthy();
  });
});
